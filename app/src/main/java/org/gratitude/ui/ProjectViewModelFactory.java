package org.gratitude.ui;

import android.arch.lifecycle.ViewModelProvider;

public class ProjectViewModelFactory extends ViewModelProvider.NewInstanceFactory {

/*    private final GratitudeDatabase mDb;
    private final Long mPrjId;

    public ProjectViewModelFactory(GratitudeDatabase database, Long projectId) {
        mDb = database;
        mPrjId = projectId;
    }

    @Override
    public <T extends ViewModel> T create(Class<T> modelClass) {
        //noinspection unchecked
        return (T) new ProjectViewModel(mDb, mPrjId);
    }*/
}
